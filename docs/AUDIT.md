# Аудит, сентябрь 2026

Проверено: весь код в `src/` вручную, контент скриптами (1159 слов, 62 урока), основные сценарии в Chromium через Playwright на ширине телефона. После исправлений: 67 тестов, валидатор без ошибок и предупреждений, сборка без предупреждений.

## Исправленные ошибки

| # | Где | Что было | Как исправлено |
|---|-----|----------|----------------|
| 1 | Урок грамматики | После урока «Перечитать теорию» → «К упражнениям» экран падал (белый экран). Воспроизведено в браузере. | Очередь заданий начинается заново |
| 2 | Урок слов | Повтор уже пройденного урока снова давал бонус 15 монет и засчитывал «урок», а оценки SM-2 ставились выученным словам досрочно и раздували интервалы | Оценки ставятся только новым словам, бонус только если были новые слова |
| 3 | Недельный челлендж | «Пройти 5 уроков грамматики» закрывался пятью проходами одного урока | Считаются только впервые пройденные уроки |
| 4 | Настройки | Если снизить дневную цель ниже уже набранного XP, стрик засчитывался только после следующего ответа | Цель проверяется сразу при смене |
| 5 | Экран локации | Кнопка «Улучшить» была неактивна, хотя монет вместе с накопленным доходом здания хватало (доход собирается автоматически при улучшении) | Накопленный доход учитывается |
| 6 | После обновления сайта | Открытая старая вкладка запрашивала файлы слов, которых на сервере уже нет: экран локации оставался пустым | Приложение перезагружается на новую версию при такой ошибке |
| 7 | Повторение | Если слово когда-нибудь удалят из контента, его карточка навсегда висела бы в счётчике «Повторить» | Такие карточки удаляются при открытии повторения |
| 8 | Озвучка | В Chrome фраза иногда не звучала: `speak()` сразу после `cancel()` теряется, а фразу без ссылки может собрать сборщик мусора | Небольшая пауза после отмены, ссылка на текущую фразу хранится |
| 9 | «Соберите фразу» | В двух примерах-диалогах появлялась плитка «—No» с тире | Тире убираются при разбиении фразы и при проверке ответа |
| 10 | Главный экран | Счётчик «Повторить» не обновлялся после полуночи, если приложение не закрывали | Пересчёт раз в минуту |
| 11 | Весь интерфейс | Любая ошибка рендера оставляла белый экран без выхода | Экран «Что-то пошло не так» с кнопкой «В город», прогресс не теряется |
| 12 | Урок слов | Теоретически двойное нажатие «Дальше» на последнем шаге могло дважды засчитать урок | Завершение срабатывает один раз |
| 13 | Контент | Пример к `querer` не содержал слова; у `la costa` и `el litoral` одинаковый перевод | Пример «¿Qué va a querer?», перевод уточнён. Валидатор теперь предупреждает о совпадающих переводах внутри уровня |

## Что можно улучшить

Ориентировочная оценка: S — до часа, M — несколько часов, L — день и больше.

### Обучение

1. **Грамматика B1.1 и B1.2** (L). Subjuntivo, отрицательный императив, будущее, условное наклонение. Районы уже есть на карте с пометкой «скоро».
2. **Упражнение на слух** (M). Звучит слово или фраза, нужно выбрать или написать ответ. Озвучка уже есть, это самый заметный пробел: сейчас нет ни одного задания на аудирование.
3. **Тренажёр спряжений** (M). Глагол, лицо и время в случайном сочетании, ввод формы. Закрепляет таблицы из A1 и A2.
4. **Ошибки в грамматике в повторение** (M). Задания, где ошибся, возвращаются через SRS, как слова.
5. **Кнопка «Трудные слова»** (S). Отдельная сессия из слов с 2+ ошибками (поле `lapses` уже есть).
6. **Лимит новых слов в день** (S). Чтобы повторения не копились лавиной после активного дня.
7. **FSRS вместо SM-2** (M). Более точные интервалы, меньше лишних повторений.
8. **Примеры для латиноамериканского варианта** (M). Сейчас в режиме es-419 слово меняется (`el jugo`), а пример остаётся испанским («Un zumo…»).
9. **Подсказка при вводе** (S). Первая буква по кнопке, с пониженной оценкой SM-2.

### Интерфейс

10. **«Продолжить» на главной** (S). Одна кнопка ведёт к следующему непройденному уроку, не надо искать его в локации.
11. **Сворачивать пройденные уровни** (S). С пятью уровнями экран локации стал длинным, текущий урок уезжает вниз.
12. **Первый запуск** (S). Два экрана: как устроен город и выбор дневной цели.
13. **Тёмная тема** (M).
14. **Фильтры в «Моих словах»** (S). По локации, по трудности, по дате повторения.
15. **Прогноз повторений** (S). В профиле: сколько слов придёт на повторение в ближайшие 7 дней.
16. **Доступность** (M). `lang="es"` на испанских строках (экранные читалки произносят их русским голосом), контраст мелкого серого текста.
17. **Напоминания** (M). На Android через уведомления PWA. Ограничение: без сервера доступны только локальные уведомления, пока приложение не выгружено из памяти.

### Техника

18. **E2E-тесты в CI** (M). Сценарии Playwright, которыми я проверял приложение, лежат вне репозитория. Их стоит перенести и запускать в GitHub Actions перед деплоем.
19. **Ленивая загрузка грамматики** (S). Все 62 урока сейчас в основном чанке (около 80 КБ), их можно грузить по районам.
20. **ESLint** (S). Линтера в проекте нет.
21. **Проверка на iOS** (S). Озвучка запускается после отрисовки кадра, а Safari может требовать, чтобы звук начинался прямо в обработчике нажатия. На Android это работает, на iPhone не проверялось.
22. **Вычитка контента носителем** (L). Слова и уроки проходят автоматическую проверку, но живой человек их не читал.

## Второй аудит (версия 1.5.1)

Проверено: код, добавленный после первого аудита (задания на слух, обновления, карта грамматики), все 112 уроков скриптами, выборочно смысл примеров. В браузере прогнаны урок с заданиями на слух, повторение, офлайн, город, блиц, грамматика и оба сценария обновления.

| # | Где | Что было | Как исправлено |
|---|-----|----------|----------------|
| 1 | Кнопка «Обновить» | Если новая версия не успевала скачаться за 15 секунд, приложение перезагружалось на старую, и плашка появлялась снова | Ожидание до минуты; если не скачалось, плашка «Не удалось скачать обновление · Ещё раз» |
| 2 | Кнопка «Обновить», первый запуск | После исправления №1 при первом запуске (страница ещё не под управлением service worker) новая версия активируется сама, а приложение минуту ждало «ожидающую» версию и сообщало об ошибке. Найдено браузерным тестом | Такой случай обрабатывается отдельно: сразу перезагрузка, она загружает новую версию |
| 3 | Задания на слух | На телефоне без испанского голоса они молчали, и пройти их было нельзя | Без испанского голоса заданий на слух нет; в настройках подсказка, как установить голос |
| 4 | Контент | «El aula está en el segundo piso» переведено как «на втором этаже», хотя в Испании это третий. Так же в двух примерах с planta | Примеры переписаны через planta baja и esta planta |
| 5 | Грамматика A1, урок 6 | Два задания подряд с одинаковой формулировкой «Выберите правильный вариант:» | Формулировки различаются: «город», «проблема» |

Проверки, которые ничего не нашли: незакрытая разметка `**` в теории, формы vosotros вне пометки `region: "es"`, двойные слова и пробелы в предложениях с пропуском, совпадение чисел в примерах и переводах.

Остаётся открытым из списка выше: все 112 уроков грамматики лежат в основном чанке (378 КБ, 101 КБ в gzip). Предупреждения о размере нет, но ленивая загрузка по районам ускорит первый запуск на медленном телефоне.
